import { TipoproductoPipe } from './tipoproducto.pipe';

describe('TipoproductoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoproductoPipe();
    expect(pipe).toBeTruthy();
  });
});
